# Plan de Migración — QueseProduce (Supabase + Cloudflare)
*Última actualización: 20 jun 2026*

> Pegá este archivo completo al inicio de futuras conversaciones para dar contexto rápido del estado de la migración.

## Arquitectura objetivo
- **Frontend:** `index.html` (single-file) servido por Cloudflare Pages, deploy automático desde GitHub.
- **Repo:** `cristobalroman95/queseproduce`, branch `main`.
- **Backend:** Supabase (Postgres + Auth). Conexión directa navegador → Supabase vía `supabase-js`. No hay Cloudflare Functions/Workers todavía.
- **Seguridad:** depende 100% de RLS, porque la `anon key` queda pública en el código fuente del HTML.

## Archivos clave del repo
- `index.html` — versión en producción (la que sirve Cloudflare Pages). Tiene el cliente Supabase, login y la tabla `shows` migrados.
- `QueseProduce_2026_rediseño_4_3.html` — versión vieja, pre-Supabase (login con PIN). **No se usa en el deploy.** Candidata a borrar o archivar.
- Cliente Supabase: línea ~1016-1018 de `index.html` (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `const sb = supabase.createClient(...)`).
- `loadShows()` línea ~1323, `saveShow()` línea ~2276, `saveShows()` línea ~2313.

## Estado por tabla (al 20 jun 2026)

| Tabla | Filas en DB | ¿Conectada al front? | Notas |
|---|---|---|---|
| `perfiles` | — | ✅ Sí | usada en login: rol, nombre, vencimiento |
| `sesiones` | — | ✅ Sí (solo insert) | log de logins |
| `shows` | 18 | ✅ Sí | `loadShows()` hace `select`, `saveShows()` hace `insert` (nuevos, sin id) + `upsert onConflict:"id"` (existentes). Requirió `alter table shows alter column id set generated by default;` para que el insert sin `id` funcione. |
| `ficha_tecnica` | 18 | ❌ No | front la sigue autocompletando con `defaultFichaTecnica()` dentro de `loadShows()` |
| `roadmap_secciones` | 83 | ❌ No | front usa `freshRoadmapFromPreset()` |
| `roadmap_tasks` | 290 | ❌ No | idem, va junto con `roadmap_secciones` |
| `presupuesto_items` | 203 | ❌ No | front usa `defaultPresupuesto()` |
| `cierre_items` | 252 | ❌ No | front usa `defaultCierre()` |
| `contenido_digital` | 10 | ❌ No | sigue en `localStorage` (`CD_STORAGE_KEY`) |
| fotos/media | no existe tabla | ❌ No | hoy se guardan como dataURL en `localStorage` |

**Punto clave:** `shows` ya lee y escribe en Supabase de punta a punta. El resto de las tablas siguen pobladas en la DB pero el frontend todavía no las lee ni las escribe — sigue generando esos datos localmente (`default*()` / `fresh*()`) y guardándolos solo en `localStorage` o en memoria.

## Orden de migración recomendado
1. ~~Verificar/configurar RLS en las 7 tablas de datos~~ — pendiente confirmar si quedó hecho para `shows`; replicar criterio en las que siguen
2. ~~`shows` — reemplazar `DEFAULT_SHOWS` por fetch a Supabase~~ ✅ **Hecho**
3. **`ficha_tecnica` (relación 1:1 con shows) ← PASO ACTUAL**
4. `roadmap_secciones` + `roadmap_tasks` (1:N, uso diario)
5. `presupuesto_items`
6. `cierre_items`
7. `contenido_digital`
8. *(futuro)* migrar fotos de `localStorage` dataURL a Supabase Storage

Razón del orden: todo depende de `show_id` (FK a `shows`), así que se validó el patrón lectura/escritura ahí primero (ya andando) y ahora se replica en las demás.

## Patrón de código a seguir en cada paso (validado con `shows`)
- Reemplazar la función `load*()`/generación local (`default*()`, `fresh*()`) que llena el dato por una versión `async` con `sb.from(tabla).select("*")`, filtrando por `show_id` cuando corresponda.
- Las funciones `save*()` separan registros **nuevos** (sin `id`) de **existentes** (con `id`): `insert(nuevos).select()` y reasignar el `id` real devuelto al objeto local; `upsert(existentes, {onConflict:"id"})` para el resto.
- Antes de insertar sin `id` en una tabla nueva, correr el equivalente a:
  ```sql
  alter table <tabla> alter column id set generated by default;
  ```
  (si la PK es `GENERATED ALWAYS AS IDENTITY`, el insert sin `id` falla).
- Errores: mostrar el toast existente (`#toast`) en vez de fallar silenciosamente — ya implementado así en `saveShows()`.
- Decidir si `localStorage` se mantiene como cache de lectura o se elimina del todo (pendiente, sigue sin resolver).

## Decisiones pendientes
- ¿RLS activado en las 7 tablas? ¿con qué policies por rol? (confirmar específicamente para `shows`, ya que está en producción)
- ¿Se mantiene `localStorage` como fallback offline o se saca del todo?
- ¿Cuándo migrar fotos a Supabase Storage?
- Mapeo de permisos por rol (`programador`, `productor`, `contador`, `tecnico`, `marketing`, `artista`) a operaciones CRUD por tabla.

## Estado de esta conversación
- **Paso actual:** Paso 3 — conectar `ficha_tecnica` al frontend (reemplazar `defaultFichaTecnica()`).
- **Próximo paso:** una vez `ficha_tecnica` ande, seguir con `roadmap_secciones` + `roadmap_tasks`.